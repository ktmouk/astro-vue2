{
  "name": "astro-vue2",
  "version": "0.0.0",
  "scripts": {
    "dev": "turbo dev --no-cache",
    "format": "turbo format",
    "build": "turbo build",
    "preview": "turbo preview"
  },
  "devDependencies": {
    "turbo": "^1.7.3"
  }
}
